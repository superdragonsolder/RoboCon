#!/usr/bin/env python3
"""
R2 比赛任务脚本 — 对抗区（三区）九宫格得分
===========================================
在对抗区将梅林中收集的 R2 KFS 放入九宫格（3×3 架子）得分。
R1 使用组装好的兵器移除对方的 KFS。

规则得分:
  九宫格底层: R1=40分  R2=50分
  九宫格中层: R1=60分  R2=70分
  九宫格顶层: R1=90分  R2=100分
  "武术大师": 3个KFS排成竖列或对角线（至少1个R2 KFS）→ 立即获胜

┌─────────────────────────────────────────────────────────────┐
│  使用方式:                                                   │
│  1. 先启动行为树:  python3 ~/connect/r2_behavior_tree.py      │
│  2. 再运行本脚本:  python3 ~/connect/mission_duikang.py       │
│                                                             │
│  紧急停止: 按 Ctrl+C 会发送急停指令                            │
└─────────────────────────────────────────────────────────────┘
"""

from r2_actions import *


# ══════════════════════════════════════════════════════════════
#   ★★★  对抗区任务序列  ★★★
# ══════════════════════════════════════════════════════════════

def 执行任务序列():
    """
    ★ 对抗区（三区）九宫格得分 ★

    动作函数速查 (来自 r2_actions.py):
    ┌──────────────┬──────────────────────────────────┐
    │ 函数          │ 说明                             │
    ├──────────────┼──────────────────────────────────┤
    │ 急停()        │ 立即制动                         │
    │ 前进(d)       │ 前进 d 米 (负数=后退)            │
    │ 左移(d)       │ 左移 d 米 (负数=右移)            │
    │ 旋转(deg)     │ 旋转 deg° (正=左转, 负=右转)     │
    │ 组合移动(f,l,r)│ 前进f + 左移l + 旋转r°           │
    │ 机械臂(n)     │ 0=空闲 1=绘制KFS                 │
    │ 夹爪(n)       │ 0=夹持角 1=闭合 2=复位 3=释放    │
    ├──────────────┼──────────────────────────────────┤
    │ 等待秒(s)     │ 等待 s 秒                        │
    │ 等待前进完成(d)│ 等待直线移动完成(+缓冲)          │
    │ 等待旋转完成(a)│ 等待旋转完成(+缓冲)              │
    └──────────────┴──────────────────────────────────┘
    """
    print("=" * 60)
    print("  对抗区（三区）九宫格得分 任务启动")
    print(f"  开始时间: {_ts()}")
    print("=" * 60)

    # ── 初始化 ──
    急停()
    等待秒(0.5)

    # ══════════════════════════════════════════════════════
    #  TODO: 在这里编写对抗区九宫格放置流程
    #
    #  参考步骤:
    #    1. 从 (5,1) 进入对抗区（上坡道）
    #    2. 导航到九宫格前
    #    3. 放置 R2 KFS 到九宫格目标格子
    #       - 机械臂(1) → 夹爪(3) 释放 → 机械臂(0)
    #    4. 如需，R1 用兵器移除对方 KFS
    #    5. 争取"武术大师"（竖列/对角线 3 个 KFS）
    # ══════════════════════════════════════════════════════

    # ── 完成 ──
    急停()
    print("\n" + "=" * 60)
    print(f"  ✅ 对抗区任务完成! 结束时间: {_ts()}")
    print("=" * 60)


# ══════════════════════════════════════════════════════════════
# 入口
# ══════════════════════════════════════════════════════════════

if __name__ == "__main__":
    print("""
╔══════════════════════════════════════════════════════════╗
║         R2 比赛任务脚本 — 对抗区（三区）九宫格              ║
║                                                          ║
║  前提: 行为树已启动 (python3 r2_behavior_tree.py)         ║
║  紧急: 按 Ctrl+C 急停                                    ║
╚══════════════════════════════════════════════════════════╝
""")
    input("按 Enter 开始执行对抗区任务...")
    执行任务序列()
