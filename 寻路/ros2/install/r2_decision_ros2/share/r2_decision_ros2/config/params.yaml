launch_manager_node:
  ros__parameters:
    cold_start: true
    rebuild_weapon: false
    recollect_mf_kfs: false

mission_state_node:
  ros__parameters:
    tick_hz: 5.0

safety_guard_node:
  ros__parameters:
    guard_touch_fake_kfs: false
    guard_touch_r1_kfs: false
    guard_non_adjacent_pick: false
    guard_step_on_kfs_cell: false
